name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - 'release/**'

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Placeholder for CI job"

  cd:
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - uses: actions/checkout@v3
      - run: echo "Placeholder for CD job"
